<%= form_for @type_of_house, :url => url_for(:controller => 'admin/settings', :action => 'save_house') do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="action">
    <%= f.submit "Invia" %>
  </div>
<% end %>

<%= form_for @type_of_contract, :url => url_for(:controller => 'admin/settings', :action => 'save_contract') do |f| %>
<h3>Nuovo tipo di contratto</h3>
<div class="field">
  <%= f.label :name %><br />
  <%= f.text_field :name %>
</div>
<div class="action">
  <%= f.submit "Invia" %>
</div>
<% end %>
